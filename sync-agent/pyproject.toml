[project]
name = "sync-agent"
version = "0.1.0"
description = "Import external news/magazine articles into deepavraman.com"
requires-python = ">=3.10,<3.14"
dependencies = [
    "requests ~= 2.32",
    "beautifulsoup4 ~= 4.12",
    "lxml ~= 5.1",
]

[tool.uv]
package = false
